{
  "name": "vterm",
  "version": "0.7.0",
  "private": true,
  "description": "VT terminal emulator monorepo — vt100.js (strict VT100) + vt220.js (VT220) + vterm.js (modern)",
  "homepage": "https://github.com/beorn/vterm",
  "license": "MIT",
  "author": "Bjørn Stabell",
  "repository": "github:beorn/vterm",
  "workspaces": [
    "packages/*"
  ],
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc -p packages/vt100 --noEmit && tsc -p packages/vt220 --noEmit && tsc -p packages/vterm --noEmit",
    "fmt": "oxfmt --write .",
    "fmt:check": "oxfmt --check ."
  },
  "devDependencies": {
    "oxfmt": "0.38.0",
    "tsdown": "^0.22.5",
    "typescript": "^5.9.3",
    "vitest": "^4.1.10"
  },
  "engines": {
    "node": ">=23.6.0"
  }
}
